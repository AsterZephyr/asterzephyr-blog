---
import type { CollectionEntry } from 'astro:content';
interface Props { post: CollectionEntry<'posts'>; }
const { post } = Astro.props;
const { title, date, summary, tags, cover, pinned } = post.data;
const formattedDate = date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' });
---
<article class="journal-entry">
  <div class="journal-meta">
    <time class="journal-date" datetime={date.toISOString()}>{formattedDate}</time>
    {pinned && <span class="journal-pin">Pinned</span>}
  </div>
  <div class="min-w-0">
    {cover && <a href={`/blog/${post.id}/`} class="journal-cover" tabindex="-1" aria-hidden="true"><img src={cover} alt="" loading="lazy" /></a>}
    <h3 class="journal-title"><a href={`/blog/${post.id}/`}>{title}</a></h3>
    {summary && <p class="journal-summary">{summary}</p>}
    {tags.length > 0 && <div class="journal-tags">{tags.slice(0, 3).map((tag) => <a href={`/tags/${tag}/`}>{tag}</a>)}</div>}
  </div>
</article>
