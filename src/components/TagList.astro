---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---
{tags.length > 0 && (
  <div class="flex flex-wrap gap-x-5 gap-y-1">
    {tags.map((tag) => (
      <a
        href={`/tags/${tag}/`}
        class="topic-link"
      >
        {tag}
      </a>
    ))}
  </div>
)}
